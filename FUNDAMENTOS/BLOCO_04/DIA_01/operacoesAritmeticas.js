const base = 5;
const height = 8;
const area = base * height;
const perimeter = ((2 * base) + (2 * height));

console.log("Base:", base, "Altura:", height, "Área:", area);
console.log("Soma dos lados do retângulo:", perimeter);